# merge-run-td

Android MVP for a merge + lane defense game built with Kotlin and Jetpack Compose.

## Current status
- PR0 bootstrap completed: Android Compose app scaffold, CI workflow, and source-of-truth data files.

## Build
```bash
./gradlew test
./gradlew assembleDebug
```
